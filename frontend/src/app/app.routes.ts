import { Routes } from '@angular/router';
import { GuideFormComponent } from './components/guide-form/guide-form.component';

export const routes: Routes = [ {path: "guide-form", component: GuideFormComponent},];
